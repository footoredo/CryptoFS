# CryptoFS

# Requirements

`libfuse` and `libcrypto++`

    sudo apt-get install libcrypto++-dev libfuse-dev

# Install

	make build

# Usage
	
Mount a cryptofs:
	
	cryptofs dir

Umount a mounted crytofs:

	sudo umount dir

